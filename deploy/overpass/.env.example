# Kopirajte u .env pored docker-compose.yml i popunite.
# Domen koji pokazuje (A zapis) na ovaj server; Caddy sam pribavlja HTTPS sertifikat.
OVERPASS_DOMAIN=overpass.primer.rs
# Dugačak nasumičan niz; isti upišite u Vercel kao OVERPASS_KEY. Napravite ga npr. sa: openssl rand -hex 32
OVERPASS_KEY=
